import 'package:flutter/material.dart';

class ProdutoPage2 extends StatelessWidget {
  const ProdutoPage2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
